services:
    build-rms:
        build:
            context: .
            dockerfile: Dockerfile
        image: ohksoehtet/rms-docker-img:v5
        container_name: rms-docker-container
        ports:
            - "8000:5050"
